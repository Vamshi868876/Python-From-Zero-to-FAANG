# 🎯 Conditionals Playground

## Level 1: Easy

### Challenge 1
Create a variable `number = 15`. 
Write an `if` statement to check if the number is greater than 10. If it is, print "Greater than 10".

```python
# Your Code Here
```

---

### Challenge 2
Create a variable `weather = "Rainy"`.
Write an `if-else` statement. If the weather is "Rainy", print "Take an umbrella". Else, print "Wear sunglasses".

```python
# Your Code Here
```

---

## Level 2: Medium

### Challenge 3 (Even or Odd)
Create a variable `x = 7`.
Write a program to check if `x` is even or odd. 
*(Hint: Use the modulo operator `%`. If `x % 2 == 0`, it is even.)*

```python
# Your Code Here
```

---

## Level 3: Prediction Challenge

### Challenge 4
Guess the Output:
```python
a = 5
b = 10

if a > b:
    print("A is bigger")
elif a == b:
    print("Equal")
else:
    print("B is bigger")
```

---

### Challenge 5
Guess the Output:
```python
x = 100

if x > 50:
    print("Half Century")
if x == 100:
    print("Century")
```
*Hint: Notice there is no `elif`. Both are separate `if` statements!*

---

## Boss Challenge 👑

### The Nightclub Bouncer Program
Write a program that defines three variables:
- `age = 22`
- `is_drunk = False`

The rules to enter the club:
1. You must be 18 or older.
2. You cannot be drunk.

Write an `if-elif-else` or nested `if` structure that prints "Welcome to the club!" if they pass both rules, and prints a rejection message if they fail any rule.
